# Project Guidance

## Design System

Always read `DESIGN.md` before making visual or interface decisions. Fonts, colours, spacing, layout, and motion are defined there. Do not deviate without explicit approval.

## Component Distribution

The application must consume the same Draft components described by `registry.json`. Do not create a separate demo-only implementation. Keep public component dependencies and registry files accurate as features change.
